These SQL files are historical one-shot deployment bundles.

Use the active Supabase SQL sources in the root `supabase/` folder instead:

- `schema.sql`
- `rls.sql`
- `rpc.sql`
- `seed-medications.sql`
